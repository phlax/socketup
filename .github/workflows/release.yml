name: release

on:
  workflow_dispatch:

permissions: {}

jobs:
  release-stub:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release workflow scaffold will be expanded in a follow-up PR."
